{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to rs2client",
            "type": "cppvsdbg",
            "request": "attach",
            "processId": "${command:pickProcess}",
        }
    ]
}
